{
  "name": "ost-docs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "mint dev",
    "build": "mint build",
    "check:links": "mint broken-links",
    "version": "mint version"
  }
}
